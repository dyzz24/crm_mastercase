import { InnerTextPipe } from './inner-text.pipe';

describe('InnerTextPipe', () => {
  it('create an instance', () => {
    const pipe = new InnerTextPipe();
    expect(pipe).toBeTruthy();
  });
});
