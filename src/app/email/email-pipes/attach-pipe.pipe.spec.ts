import { AttachPipePipe } from './attach-pipe.pipe';

describe('AttachPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AttachPipePipe();
    expect(pipe).toBeTruthy();
  });
});
